export enum Message {
  invalidUserId = 'Invalid userId',
  userNotFound = 'User not found',
  missingRequiredFields = 'Missing required fields',
  internalServerError = 'Internal server error',
  invalidRoute = 'Invalid route'
}

export const numberId = 3;